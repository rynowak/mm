[project]
name = "mm-tokenizers"
version = "0.1.0"
description = "Character-level and BPE tokenizers"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mm_tokenizers"]
