[project]
name = "mm-tetris"
version = "0.1.0"
description = "Tetris game environment with placement-based action space for RL"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mm_tetris"]
